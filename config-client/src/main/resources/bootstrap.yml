spring:
  cloud:
    config:
      uri: http://localhost:8080
      enabled: true
  application:
    name: ClientApp

server:
  port: 8086

management:
  endpoints:
    web:
      exposure:
        include: '*'
